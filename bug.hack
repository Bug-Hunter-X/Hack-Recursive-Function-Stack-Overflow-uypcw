function foo(x: int): int {
  if (x == 0) {
    return 1;
  } else {
    return x * foo(x - 1);
  }
}

function main(): void {
  echo foo(5);
}

This code will produce a stack overflow error for larger inputs because it recursively calls itself without a base case. The base case should be x <=0 instead of only x ==0